# deploy-nextcloud

Deploy Nextcloud with docker compose.

```bash
./run.sh 
Usage:
./run.sh -c <cmd> -l "<item list>"
eg:
./run.sh -c deploy -l "nextcloud"
Supported cmd:
deploy
Supported items:
nextcloud
```

